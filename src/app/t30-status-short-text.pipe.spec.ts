import { T30StatusShortTextPipe } from './t30-status-short-text.pipe';

describe('T30StatusShortTextPipe', () => {
  it('create an instance', () => {
    const pipe = new T30StatusShortTextPipe();
    expect(pipe).toBeTruthy();
  });
});

import { T30StatusLongTextPipe } from './t30-status-long-text.pipe';

describe('T30StatusLongTextPipe', () => {
  it('create an instance', () => {
    const pipe = new T30StatusLongTextPipe();
    expect(pipe).toBeTruthy();
  });
});

import { T30StatusStarSvgUrlPipe } from './t30-status-star-svg-url.pipe';

describe('T30StatusStarSvgUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new T30StatusStarSvgUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
